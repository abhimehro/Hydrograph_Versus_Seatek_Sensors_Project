## 2024-06-02 - Testing `format_sensor_name` Function
**Learning:** When adding tests, make sure you carefully read the implementation of the target function and ensure your tests assert the actual return values (especially for edge cases), instead of assuming what the function should do. The initial test implementation failed because it made incorrect assumptions about the output.
**Action:** Always verify actual behavior and test against that behavior, or explicitly refactor the target code if the behavior is deemed incorrect.
