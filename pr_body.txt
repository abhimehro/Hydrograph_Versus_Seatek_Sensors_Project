**QA Check Complete**

- Code builds correctly.
- All tests passing (`pytest tests/ -v`).
- Automated pre-commit hooks (Black/Isort) applied successfully to enforce repository style conventions.

**Commands Executed:**
```bash
python3 -m pytest tests/ -v
pre-commit run --all-files
```

═════════ ELIR ═════════
PURPOSE: Applied code formatting (black/isort) across the codebase.
SECURITY: N/A, format changes only.
FAILS IF: Linter rules conflict with formatting.
VERIFY: Tests pass and code looks properly styled.
MAINTAIN: Run pre-commit before committing future code changes.
