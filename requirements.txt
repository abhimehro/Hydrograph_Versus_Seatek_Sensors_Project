# Runtime dependencies - exact pins matching pyproject.toml [tool.poetry.dependencies]
# Align with pyproject.toml / poetry.lock (numpy 2.x); previously capped at <2.0
numpy==2.2.6
pandas==2.3.3
matplotlib==3.10.9
# DEPENDENCY: pillow>=12.3.0 — explicit floor for matplotlib transitive;
# Pillow 12.2.0 has OSV PYSEC-2026-2253..2257 / 3451..3453 (fixed in 12.3.0)
pillow==12.3.0
seaborn==0.13.2
openpyxl==3.1.5
colorlog==6.12.0
pyyaml==6.0.3
defusedxml==0.7.1
