# Core dependencies
numpy~=2.2.0
pandas~=2.2.3  # Latest stable version
matplotlib>=3.7.3  # Stable version, ensure compatibility
seaborn>=0.13.0  # Compatible with latest pandas

# Visualization tools
pillow~=11.0.0
colorlog~=6.9.0  # Format/handle colored logs
visualization~=1.0.0

# Excel file support
openpyxl>=3.1.3  # Stable version; avoids pre-release

# Testing dependencies
pytest>=7.4.2  # Latest stable, essential for testing
attrs>=24.3.0  # Flexible and dynamic class decorators

# Security and serialization
cryptography>=44.0.0
protobuf>=5.0.0  # Stable version for message serialization

# Utilities
Jinja2>=3.1.2  # Templating engine
setuptools>=68.0.0  # Ensure compatibility with Python builds

# XML processing
lxml~=5.3.0