[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pandas = ">=1.5.0"
matplotlib = ">=3.5.0"
seaborn = ">=0.11.0"
openpyxl = ">=3.0.0"
numpy = ">=1.21.0"
colorlog = ">=6.7.0"
pyyaml = ">=6.0.0"

[dev-packages]

[requires]
python_version = "3.10"
